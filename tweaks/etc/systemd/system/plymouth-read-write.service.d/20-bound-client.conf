# Keep a stalled Plymouth IPC request from holding sysinit.target indefinitely.
# The client is optional in the vendor unit, so preserve that failure policy.
[Service]
ExecStart=
ExecStart=-/usr/bin/timeout --verbose --signal=TERM --kill-after=3s 20s /usr/bin/plymouth update-root-fs --read-write
TimeoutStartSec=25s
